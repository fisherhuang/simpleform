import { styled } from "@mui/material/styles";
import { Grid2, FormLabel as MFormLabei } from "@mui/material";

export const FormGrid = styled(Grid2)(() => ({
  display: "flex",
  flexDirection: "column",
}));

export const FormLabel = MFormLabei;

export const MainGrid = Grid2;
